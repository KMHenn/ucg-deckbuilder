Running the project
- Run `composer install` and `npm install`
- Run migrations `php artisan migrate`
- Run card sync command `php artisan app:sync-card-data`
- Run `npm run dev` and navigate to browser
